# Keep Colab's CUDA-matched torch build. These versions match the supplied
# first-place notebook where applicable.
numpy>=1.26,<3
pandas>=2.0,<3
transformers==4.56.2
huggingface_hub>=0.34,<1.0
kaggle>=1.6,<2
pytest>=8,<9
